<h1>Magazine View</h1>

<h2><%= label_tag 'magazine', @magazine.name %></h2>

<h3>Subscribers</h3>
<% @magazine.subscribers.each_with_index do |subscriber, index| %>

	<%= "Subscriber #{index+1} : #{subscriber.name}" %><br>

<br>
<% end %>
<%= link_to 'Edit', edit_magazine_path(@magazine) %>
&emsp;
<%= link_to 'DELETE', magazine_path(@magazine), method: :delete, data: { confirm: "Are You sure to Delete \"#{@magazine.name}\""} %> 

<br><br>
<br>
<%= link_to "Magazines", magazines_path %>
<br><br>
<%= link_to 'Back to Home', root_path %>